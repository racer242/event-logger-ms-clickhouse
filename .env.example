# NestJS Application
NODE_ENV=development
PORT=3000
HOST=localhost

# ClickHouse Configuration
CLICKHOUSE_HOST=localhost
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=event_logger
CLICKHOUSE_MAX_CONNECTIONS=10
# Отключение проверки подключения к БД при старте (true/false)
CLICKHOUSE_SKIP_HEALTH_CHECK=false
# Async insert settings (0/1)
CLICKHOUSE_ASYNC_INSERT=1
CLICKHOUSE_WAIT_FOR_ASYNC_INSERT=0

# Redis Configuration (Optional)
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
QUEUE_PREFIX=event_logger
# Отключение проверки подключения к Redis при старте (true/false)
REDIS_SKIP_HEALTH_CHECK=false

# Buffer Configuration
BUFFER_MAX_SIZE=1000
BUFFER_FLUSH_INTERVAL_MS=5000

# Security
API_KEY_HEADER=X-API-Key
API_KEYS=dev-api-key-12345,prod-api-key-67890

# Throttling (requests per minute)
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# Logging
LOG_LEVEL=debug

# Swagger
SWAGGER_ENABLED=true
SWAGGER_PATH=/api/docs
